fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
psutil>=5.9         # reliable subprocess-tree kill on cancel
# OSINT engines (installed as CLI tools, called via subprocess)
sherlock-project
maigret
holehe
socialscan            # username+email definitive existence checks
ignorant              # phone-number registration checks
phonenumbers          # split a phone into country-code + national (for ignorant)
# phoneinfoga & gosearch are Go binaries — fetch via scripts/fetch-binaries.ps1
